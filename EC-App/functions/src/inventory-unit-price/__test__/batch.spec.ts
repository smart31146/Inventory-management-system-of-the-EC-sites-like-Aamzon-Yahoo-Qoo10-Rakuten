import { describe, it, jest } from '@jest/globals';
import { initTesting } from '../../helper/test-util';
import { inventoryUnitPriceBatch } from '../batch';

jest.setTimeout(120000);

initTesting();

describe('batch of inventory unit price', () => {
  it('Unit test: ', async () => {
    //await inventoryUnitPriceBatch(new Date('2023/12/31UTC+9'));
  });
});
