import { initTesting } from '../test-util';
import { getCsvFromStorage } from '../storage/getCsvFromStorage';

jest.setTimeout(10000);

initTesting();

describe('helper function', () => {
  it('getCsvFromStorage', async () => {
    //const csv = await getCsvFromStorage('c006', new Date('2023-12-06'), {
    //  fromLine: 7,
    //});
    //console.log(csv[0]);
  });
});

