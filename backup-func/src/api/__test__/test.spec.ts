import { initTesting } from "../../helper/test-util";
import { describe, it, jest } from '@jest/globals';
import { TestOutputSpreadSheet } from '../batch/test_outputSpreadSheet';

jest.setTimeout(50000);

initTesting();


describe('batch of output test spreadsheet', () => {
    it('Unit test', async () => {

        //await TestOutputSpreadSheet(0);
        
    });
});