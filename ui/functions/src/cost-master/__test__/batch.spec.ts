import { describe, it, jest } from '@jest/globals';
import { initTesting } from '../../helper/test-util';
import { costMasterBatch } from '../batch';

jest.setTimeout(10000);

initTesting();

describe('batch of cost master', () => {
  it('Unit test', async () => {
    // await salesChannelMasterBatch();
  });
});
